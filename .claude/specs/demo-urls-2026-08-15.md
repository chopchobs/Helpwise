# URL ของทุก tenant สำหรับถ่ายภาพหน้าจอ (2026-08-15)

> ที่มาของทุกค่าในไฟล์นี้ — **อ่านจากโค้ด ไม่ได้จำมา**
> · slug + persona: `src/lib/demo-personas.ts` (single source of truth)
> · password: `src/lib/demo.ts` (`DEMO_PASSWORD`)
> · contact: `prisma/seed-demo.ts`
> · รูปแบบ host: `src/proxy.ts:203,216` (`NEXT_PUBLIC_ROOT_DOMAIN` default `gethelpwise.xyz`)
>
> ⚠️ ถ้าไฟล์ต้นทางเปลี่ยน ไฟล์นี้จะเก่าทันทีโดยไม่มีอะไรเตือน — **เช็คกับต้นทางก่อนใช้เสมอ**

---

## Tenant ที่มีอยู่

| slug | ชื่อ | plan |
| --- | --- | --- |
| `acme` | Acme Inc | `pro` |
| `globex` | Globex Corp | `pro` |

> ทั้งสอง tenant ถูก seed เป็น `pro` เหมือนกัน (`prisma/seed-demo.ts:590,622,630`)
> ⇒ **ภาพหน้าจอเทียบ plan ต่างกันถ่ายจาก demo นี้ไม่ได้** — ต้องแก้ plan เองก่อน

**prod host:** `https://<slug>.gethelpwise.xyz`
**dev host:** `http://<slug>.localhost:3000`

---

## 1. ฝั่ง agent (ต้อง login)

| หน้า | path | หมายเหตุ |
| --- | --- | --- |
| **Demo entry (1 คลิก)** | `/demo` | 🔑 **เริ่มที่นี่** — auto-login แล้ว redirect · `?persona=primary\|secondary` สลับตัวละคร · `?next=<path>` ระบุปลายทาง |
| Login (กรอกเอง) | `/login` | ใช้ตอนอยากถ่ายหน้า login เอง |
| Dashboard | `/dashboard` | |
| Ticket list | `/tickets` | |
| Ticket detail | `/tickets/<id>` | id ต้องหยิบจาก list — ไม่ใช่เลขคงที่ |
| Ticket ใหม่ | `/tickets/new` | |
| Reports | `/reports` | gate ด้วย flag `analytics` |
| Settings · SLA | `/settings/sla` | gate ด้วย flag `sla_policies` |
| Settings · Tags | `/settings/tags` | |
| Settings · Canned responses | `/settings/canned-responses` | |
| Settings · Branding | `/settings/branding` | **หน้าที่เห็นผลกับ portal** |
| Settings · Billing | `/settings/billing` | |
| Settings · API keys | `/settings/api-keys` | |
| Settings · Webhooks | `/settings/webhooks` | |

### URL เต็มที่ใช้ได้เลย

**prod**
```
https://acme.gethelpwise.xyz/demo
https://acme.gethelpwise.xyz/demo?persona=secondary
https://acme.gethelpwise.xyz/demo?next=/reports
https://globex.gethelpwise.xyz/demo
https://globex.gethelpwise.xyz/demo?persona=secondary
```

**dev** (`npm run dev`)
```
http://acme.localhost:3000/demo
http://acme.localhost:3000/demo?persona=secondary
http://globex.localhost:3000/demo
http://globex.localhost:3000/demo?persona=secondary
```

---

## 2. บัญชีที่ล็อกอินได้

> ⚠️ credentials ชุดนี้ **เป็นสาธารณะโดยตั้งใจ** (portfolio demo) และถูกจำกัดเป็น `role = AGENT`
> เท่านั้น — ไม่ใช่ `OWNER`/`ADMIN` (บังคับซ้ำทั้งที่ route และ seed) ⛔ ห้ามเอา pattern นี้ไปใช้กับ user จริง

| tenant | persona | email | วิธี login |
| --- | --- | --- | --- |
| acme | `primary` — Demo Agent | `demo@acme.helpwise.com` | **password** `demo-helpwise-2026` หรือ `/demo` |
| acme | `secondary` — Alex Rivera | `alex@acme.helpwise.com` | 🔑 **`/demo?persona=secondary` เท่านั้น** — ไม่มี password สาธารณะ (hash สุ่มตอน seed) |
| globex | `primary` — Demo Agent | `demo@globex.helpwise.com` | **password** `demo-helpwise-2026` หรือ `/demo` |
| globex | `secondary` — Dana Wu | `dana@globex.helpwise.com` | 🔑 **`/demo?persona=secondary` เท่านั้น** |

> ⚠️ email ของ agent ลงท้าย `helpwise.com` (ไม่ใช่ `.xyz`) **โดยตั้งใจ** — เป็นค่าใน seed
> อย่า "แก้ให้ตรงโดเมนจริง" (มี memory บันทึกไว้แล้วว่าปล่อยไว้ตั้งใจ)

---

## 3. ฝั่ง portal (ลูกค้า) — ⚠️ **ถ่ายภาพยากกว่า อ่านก่อน**

| หน้า | path |
| --- | --- |
| Portal login (ขอ magic-link) | `/portal/login` |
| Portal verify | `/portal/verify` |
| Ticket ของตัวเอง | `/portal/tickets` |
| Ticket detail | `/portal/tickets/<id>` |
| แจ้ง ticket ใหม่ | `/portal/tickets/new` |

**Contact ที่มีอยู่ใน seed** (ใช้กรอกในหน้า `/portal/login`):

| tenant | contact |
| --- | --- |
| acme | `jane.cooper@northwind.example` · `marcus.lee@umbra.example` · `priya.nair@lumon.example` · `tom.becker@globex.example` |
| globex | `sara.kim@initech.example` · `omar.haddad@hooli.example` · `lena.fischer@piedpiper.example` |

> 🔴 **portal ไม่มี one-click demo** — login เป็น **magic-link ทางอีเมล** และโดเมน `.example`
> เป็นโดเมนสงวนที่ **รับเมลไม่ได้จริง** ⇒ ถ่ายหน้า `/portal/tickets` บน prod **ไม่ได้** ด้วยวิธีปกติ
> · ถ่ายได้แน่ ๆ คือ **`/portal/login`** (หน้าเปล่า + branding ของ tenant)
> · ถ้าต้องการหน้าหลัง login จริง ๆ ต้องหยิบ token จาก DB/log บน dev — **ยังไม่มีขั้นตอนที่เขียนไว้**
>
> 🔑 คุณค่าของ portal ในภาพหน้าจอคือ **branding ต่อ tenant** (logo + accent) ⇒ เทียบ
> `acme` กับ `globex` ที่หน้า `/portal/login` เห็นความต่างได้โดยไม่ต้อง login

---

## 4. หน้า marketing (ไม่มี tenant — อยู่บน root domain)

```
https://gethelpwise.xyz/           (landing)
https://gethelpwise.xyz/signin
https://gethelpwise.xyz/signup
```
dev: `http://localhost:3000/` · `/signin` · `/signup`

---

## 5. ข้อควรระวังตอนถ่ายภาพ

- **`acme` มีข้อมูลขยะจาก dev/smoke ปนอยู่** — agent เห็นทั้ง tenant ⇒ ticket ที่ไม่ได้อยู่ใน seed
  (`#1001`–`#1007`) จะโผล่ในภาพด้วย · **`globex` สะอาดกว่า** ถ้าต้องการภาพที่คุมได้
- **`.localhost` subdomain** ใช้ได้เลยบน Chrome/Safari สมัยใหม่ ไม่ต้องแก้ `/etc/hosts`
- ค่าใน `/api/health/readiness` **ไม่ใช่หน้าจอสำหรับถ่าย** — เป็น ops endpoint (ตอบ JSON)
