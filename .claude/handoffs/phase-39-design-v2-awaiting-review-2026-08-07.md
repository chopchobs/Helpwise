# Phase 39 — design doc v2 มาแล้ว รอรีวิว (ปิดวัน 2026-08-07)

## สถานะ

- **main = `6c50277`** — `docs(spec): design doc Phase 39 v2 — แก้ตามรีวิว 9 ข้อ · ยังไม่ตัดสิน`
  · **ยังไม่ push** (ตอนเขียนไฟล์นี้ `main...origin/main [ahead 1]`; หลัง commit handoff นี้จะเป็น **ahead 2**) — Dev push เอง
- Phase 39 อยู่ขั้น **ออกแบบ ยังไม่มีโค้ดสักบรรทัด · ยังไม่ตัด branch**
- ไฟล์ที่เกี่ยวข้อง (path ครบ):

| ไฟล์ | path |
| --- | --- |
| design brief (โจทย์ + เกณฑ์ §8) | `.claude/specs/phase-39-design-brief-2026-08-06.md` |
| incident ต้นเรื่อง (QStash region) | `.claude/specs/phase-38-qstash-region-incident-2026-08-06.md` |
| design doc **v1** (เก็บไว้ ห้ามลบ) | `.claude/specs/phase-39-design-doc-2026-08-07.md` |
| design doc **v2** (ตัวที่ต้องรีวิว) | `.claude/specs/phase-39-design-doc-v2-2026-08-07.md` |

---

## ลำดับที่ทำมาแล้ว

**brief → v1 → รีวิว 9 ข้อ → v2**

v2 ระบุใน changelog ของตัวเองว่าแก้ครบทั้ง 9 ข้อ (🔴 3 · 🟠 5 · 📌 1) — โครง §0–§8 เดิม ไม่ได้รื้อ
เพิ่มหัวข้อใหม่ 3 ตัว: `Changelog v1 → v2` · `§2.5 P3 (ก/ข/ค)` · `§2.6 Supabase realtime JWT probe`
และเพิ่ม `§3 ข้อ 1ก` (ทำไม step ต่อท้าย deploy ใช้ไม่ได้)

> ⚠️ **ยังไม่ผ่านการตัดสิน** — "แก้ครบตาม changelog ของตัวเอง" ไม่เท่ากับ "ผ่านเกณฑ์"
> ยังไม่มีใครรีวิว v2 ทั้งฉบับ

---

## 4 อย่างที่ตัดสินแล้วว่าถูก — ห้ามรื้อใน v3

1. **hybrid A + B** (active probe + execution heartbeat) ไม่ใช่เลือกทางเดียว
2. **GitHub Actions เป็น scheduler** ไม่ใช่ QStash (ตัด circular dependency)
3. **แจ้งเฉพาะตอนเปลี่ยนสถานะ (transition-only)** รวมถึงแจ้งตอน recovery
4. **การประกาศช่องโหว่ 60-day auto-disable ของ GitHub Actions** เป็นความซื่อสัตย์ที่ต้องรักษาไว้

---

## เกณฑ์ตัดสิน v2 ที่ตกลงไว้ล่วงหน้า

> 🔒 **สำคัญที่สุด — ห้ามขยับเกณฑ์หลังอ่านคำตอบแล้ว**

เกณฑ์ **§8 ของ brief ทั้ง 5 ข้อ ยังใช้เหมือนเดิม** บวกเกณฑ์เฉพาะของ **ข้อ 9** (cron ข้ามรอบ + หยุดถาวร):

| คำตอบข้อ 9 | ตัดสิน |
| --- | --- |
| กลไกครอบ "ข้ามรอบชั่วคราว" + ระบุชื่อช่องว่าง "หยุดถาวร" ให้ชัด แล้วเขียนลง §8 | ✅ ผ่าน |
| pinger ฟรีตัวเล็กปิดช่วงหยุดถาวรได้ด้วย | ✅ ผ่าน (ดีกว่า) |
| "ปิดครบต้องมีตัวเฝ้านอก GitHub ซึ่งไม่ฟรี" + ระบุราคาและสิ่งที่ได้ | ✅ ผ่าน — กลายเป็นการตัดสินใจเรื่องงบของ Dev |
| เช็คลิสต์รายเดือน / ให้คนไปดู Actions tab | ❌ ตก |
| กลไกที่พึ่ง GitHub Actions ตรวจตัวเองโดยไม่ประกาศว่า circular | ❌ ตก |

**หลักเดียว:** ช่องว่างที่ถูก**ตั้งชื่อและตีกรอบแล้ว = ผ่าน** · ช่องว่างที่**ไม่ถูกพูดถึง หรือกลไกปลอมที่ปิดมันไว้ = ตก**

---

## ขั้นถัดไป

1. **รีวิว v2 ตามเกณฑ์ข้างบน** → ผ่าน / ผ่านแบบมีเงื่อนไข (คำถามงบ) / ตก
2. ถ้าผ่าน → ค่อยตัด branch **`feature/phase-39-server-env-readiness`** แล้วเริ่ม implement
3. ⚠️ ถ้า v3 ยังมี 🔴 เหลือ — **ทางออกคือลด scope ของ Phase 39** (ทำ P2 ให้จบจริง ยก P3 เป็น Phase 40)
   **ไม่ใช่วนแก้ดีไซน์รอบ 3**

---

## ของที่ยังค้างจาก Phase 38 (ยังไม่หาย)

- **`EMAIL_PROVIDER` ไม่มีบน Vercel** — เริ่มที่ "**ตัดสินว่าจะใช้ provider ไหน**" ไม่ใช่ "ตั้ง env"
- **sla-sweep ไม่มี per-tenant checkpoint**
- **ทำ P2 เสร็จต้องกลับไปอัปเดตแถว "server env / provider"** ใน `CLAUDE.md` § Post-merge gate
