# Handoff: Phase 39 — ข้อ 3 ลงโค้ดแล้ว · ปิดแถว D · E · **เหลืออีก 1 รอบแล้วจบ**
Date: 2026-08-15
Next focus: **รอบสุดท้าย** — อัปเดตตารางหลักฐานเป็น **18/21** + **story doc** → **ปิด Phase 39**

> 📌 **ไฟล์นี้เป็น pointer** — ห้ามคัดเนื้อจาก incident / closing-evidence มาสรุปซ้ำ
> ⛔ **ห้ามเดินงานต่อจากสรุปในไฟล์นี้** · ต้องการรายละเอียดเมื่อไร **เปิดต้นทางเสมอ**

---

## 🔴 กฎกันลูป — บังคับตั้งแต่ 2026-08-15 เป็นต้นไป

**ที่มา:** 2026-08-10 → 08-14 **ไม่มีรอบไหนแตะโค้ดเลย** ทุกรอบเป็นเอกสารแก้เอกสาร
⇒ ลูปไม่มีเงื่อนไขจบในตัวเอง (เอกสารที่ถูกแก้ ผลิตของให้แก้รอบต่อไปได้เสมอ)

> ⛔ **ห้ามมีรอบงานที่ผลลัพธ์เป็น "เอกสารอย่างเดียว"**
> เจอของผิดในเอกสารระหว่างทาง → **จดลง backlog · ไม่เปิดรอบใหม่ · ไม่แก้ในรอบนี้**

⚠️ **ข้อยกเว้นเดียวที่ตกลงกันแล้ว: รอบสุดท้าย** (ตาราง 18/21 + story doc) — เพราะมันคือ **การปิดเฟส** ไม่ใช่การไล่แก้

---

## Git State — verify ด้วยคำสั่งจริง 2026-08-15

```
git ls-remote origin main            → 0e2705a9f83a04e7ed6ac7950e9884407c78f95b
git rev-parse HEAD (ก่อนแตก branch)  → 0e2705a  (ตรงกับ remote)
git rev-parse --abbrev-ref HEAD      → feature/phase-39-watcher-late
```

> 🔑 **`git ls-remote origin main` ต้องอยู่ในสูตร verify ทุกครั้ง**
> `git rev-parse origin/main` อ่าน **ref ในเครื่อง** ⇒ สดเท่ากับ `fetch` ครั้งล่าสุดเท่านั้น
> การยืนยันเรื่อง remote ด้วยหลักฐานจาก local = **ตรวจผิดชั้น** (บทเรียน 2026-08-14)

- [ ] branch `feature/phase-39-watcher-late` **ยังไม่ push** — Dev เปิด PR เอง
- [ ] `.claude/evidence/` เป็นไดเรกทอรีใหม่ (ตรวจแล้วว่า commit ลง public repo ได้ — ดู README ในนั้น)

---

## สิ่งที่ลงโค้ดรอบนี้ (ครั้งแรกตั้งแต่ 2026-08-10)

1. **verdict ใหม่ `WATCHER_LATE`** — ไม่ใช้ `STALE` ซ้ำ
2. **แยกบทบาทใน `MECHANISMS`** เป็น `{ intervalSeconds, role: "watched" | "watcher" }`
   ⛔ ไม่ใช่การยกเว้นตามชื่อ
3. **ประกาศคาบให้ตรงกับที่วัดได้** — `CRON_INTERVAL_MINUTES` 15 → **60** · `cron:` → `0 * * * *`
   + ขยาย regex ใน `readiness-cadence.test.ts` ให้ derive คาบได้ 3 รูปแบบ
4. **ปิดแถว D · E** ในตารางหลักฐาน (หลักฐานคำต่อคำอยู่ในไฟล์นั้น)

→ เหตุผลทั้งหมด **เขียนอยู่ในโค้ด** (`readiness-verdict.ts` เหนือ `CRON_INTERVAL_MINUTES` ·
`readiness.ts` เหนือ `httpStatusFor()` · `heartbeat.ts` เหนือ `MECHANISMS`) — ⛔ ห้ามคัดมาซ้ำที่นี่

---

## 🔴 งานค้างที่มี **กำหนดเวลา** — ห้ามลืม

> **1 วันหลัง merge: วัดคาบซ้ำ**
> ```
> gh run list --workflow readiness.yml --limit 100 \
>   --json databaseId,event,status,conclusion,createdAt,startedAt,updatedAt > <ไฟล์ใหม่>
> node .claude/evidence/phase-39-h17-2026-08-14/analyze.mjs   # ชี้ไฟล์ใหม่
> ```
> **ทำไมสำคัญ:** ประกาศเป็นรายชั่วโมงแล้ว **GitHub จะให้อะไรกลับมาไม่มีใครวัด**
> อาจดีขึ้น เท่าเดิม หรือ **แย่ลง** ⇒ §H-14 ใช้กับตัวเราเอง: **อย่าประกาศผลก่อนวัด**
> ⚠️ ถ้าคาบจริงเปลี่ยนไปจาก ~65 นาทีมาก **ค่า 60 ที่เพิ่งตั้งจะผิดอีกรอบ** — และคราวนี้เรารู้วิธีตรวจแล้ว

**คาดการณ์ที่บันทึกไว้ล่วงหน้า (เพื่อให้ตรวจสอบได้ว่าเดาถูกหรือผิด):**
- เกณฑ์ stale ใหม่ = `60 × 60 × 3` = **3 ชม.** · max gap ที่วัดได้ = **156.9 นาที = 2.62 ชม.**
  ⇒ margin เหลือ **~23 นาที** ⇒ **คาดว่าจะยังเห็น `WATCHER_LATE` เป็นครั้งคราว — และนั่นถูกต้อง**
- `detectGap` ที่ interval 60 ยิงเมื่อ gap ≥ 120 นาที ⇒ จากข้อมูลเดิม **9/88 ช่วง (10.2%)**
  แทนที่จะเป็น **88/88 (100%)** ⇒ กลับมาเป็นสัญญาณที่มีความหมาย

---

## แผนรอบสุดท้าย (รอบเดียวจบ)

1. อัปเดตตารางหลักฐาน → **18/21** (หัวตารางอัปเดตแล้ว · เหลือส่วนบรรยาย/ยอดรวมอื่น ๆ)
2. เขียน **story doc** ของ Phase 39
3. ประกาศ **A · C · I-b เป็นของค้างที่รู้ตัว** ⛔ ไม่ใช่ของที่ลืม — ต้องเขียนว่าทำไมถึงไม่ทำ
4. ปิดเฟส

⛔ **ห้ามไล่ปิด A · C · I-b** — Dev ตัดสินแล้วว่า **ไม่ทำ**

---

## ⚠️ ของที่ยังเปิด (ยกไป Phase 40 — ห้ามเปิดรอบใหม่ในเฟสนี้)

- 🔴 **ยังไม่รู้ว่า *ทำไม* GitHub ถึง throttle repo นี้** — ปิดได้แค่ *"ไม่มีอะไรฝั่งเราที่แก้ด้วย cron expression ได้"*
- 🔴 **ทาง B ยอมรับข้อจำกัด: ตรวจจับช้า ~1 ชม. โดยธรรมชาติ** — เป็น *การยอมรับ* ไม่ใช่ข้อสรุปจากหลักฐาน
  ⇒ ทบทวนได้โดยไม่ต้องรอหลักฐานใหม่ (§9 ของ incident)
- 🔴 **เกณฑ์ของทาง B ผูกกับค่าที่ GitHub เปลี่ยนได้ตลอดโดยไม่บอกเรา** ⇒ ต้องวัดซ้ำเป็นระยะ
- **§G ข้อ 18** · **B-11** · **`readiness.ts:552` ฯลฯ ที่เป็น snapshot-age STALE** (ไม่ต้องแก้ — ดู §10.1)

---

## References
- Master plan: `.claude/project-plan.md`
- ตารางหลักฐานปิดเฟส: `.claude/specs/phase-39-closing-evidence-2026-08-09.md`
- Incident (§7 ข้อ 2 ปิดแล้ว · §10.1 คำตอบ pre-check): `.claude/specs/incident-2026-08-10-prod-503.md`
- **หลักฐาน GitHub Actions + สคริปต์คำนวณ:** `.claude/evidence/phase-39-h17-2026-08-14/`
- Erratum: `.claude/specs/phase-39-design-doc-v2.1-errata-2026-08-08.md`
- Backlog: `.claude/specs/backlog-2026-08-08.md`
- Handoff ก่อนหน้า: `.claude/handoffs/phase-39-h12-incident-2026-08-10.md`
